# WorkDesk — Frontend (React + Vite)

Estructura inicial del frontend basada en Vite + React.

Comandos:

- Instalar dependencias: `npm install`
- Ejecutar en desarrollo: `npm run dev`
- Construir: `npm run build`

Notas:

- Usa Bootstrap y Firebase; configura variables en `.env`.
